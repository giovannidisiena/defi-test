let v1,v2;_bfe‍.w("@aave/protocol-js",[["v1",["v1"],function(v){v1=v}],["v2",["v2"],function(v){v2=v}]]);

// Fetch poolReservesData from GQL Subscription
// Fetch rawUserReserves from GQL Subscription
// Fetch ethPriceUSD from GQL Subscription

const userAddress = "0x1b7835d2074914161dD6A2d48E393Be1dbf296D1";

const v1UserSummary = v1.formatUserSummaryData(
  userAddress.toLowerCase(),
  Math.floor(Date.now() / 1000)
);

const v2UserSummary = v2.formatUserSummaryData(
  userAddress.toLowerCase(),
  Math.floor(Date.now() / 1000)
);

console.log(`v1 summary ${v1UserSummary}`);
console.log(`v2 summary ${v2UserSummary}`);
